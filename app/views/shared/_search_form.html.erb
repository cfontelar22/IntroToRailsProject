<%= form_with(url: search_path, method: :get, local: true) do |form| %>
    <%= form.search_field :query, placeholder: 'Search books...', aria_label: 'Search books' %>
    <%= form.submit 'Search' %>
  <% end %>